<div id="banner" class="sign_up_banner">
	<h1>Create a Podcast. Its 100% FREE!</h1>
</div>
 <div id ="form">
	<%= simple_form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
  	<%= f.error_notification %>
  	<div class="form-inputs wrapper_skinny" >
		<%= f.input :title, autofocus: true, class: 'field' %><br>
	    <%= f.input :email, class: 'field', placeholder: 'lubwils@example.com' %><br>
	    <%= f.input :password, placeholder: ("#{@minimum_password_length} characters minimum" if @minimum_password_length), class: 'field' %><br>
	    <%= f.input :password_confirmation, class: 'field' %><br><br>

	    <div class="actions">
	    <%= f.button :submit, "Click to Sign Up", class: "button button_highlight button_block" %>
	  	</div>
	  </div> 
	<% end %>
</div>
